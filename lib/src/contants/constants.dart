import 'package:flutter/material.dart';

const s = 1;

const List navItems = [
  {'label': 'Video Feed', 'icon': Icons.mic_external_on},
  {'label': 'Snapshot', 'icon': Icons.camera_alt},
  // {'label': 'Settings', 'icon': Icons.settings_applications},
];

const Map routes = {

};